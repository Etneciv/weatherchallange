/** @type {import('next').NextConfig} */
const nextConfig = {
        images:{
            
            unoptimized:true,
            domains:['https://openweathermap.org/']
        }
        
};

export default nextConfig;
